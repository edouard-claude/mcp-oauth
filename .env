# Configuration du serveur MCP
MCP_SERVER_URL=http://localhost:8080
MCP_PORT=8080

# Configuration OAuth2
# OAUTH2_AUTHORIZATION_SERVER est optionnel :
# - Si vide ou non défini, le MCP servira de serveur d'autorisation intégré (mode autonome)
# - Si défini, le MCP utilisera ce serveur d'autorisation externe
OAUTH2_AUTHORIZATION_SERVER=
OAUTH2_CLIENT_ID=your-client-id
OAUTH2_CLIENT_SECRET=your-client-secret
OAUTH2_REDIRECT_URI=http://localhost:8080/callback
OAUTH2_SCOPES=openid

# Secret pour la signature des tokens JWT (requis si mode serveur d'autorisation intégré)
# Doit être sécurisé en production (minimum 32 caractères aléatoires)
OAUTH2_JWT_SECRET=your-secure-jwt-secret-here

# Secret pour la génération des sessions (doit être sécurisé en production)
MCP_SESSION_SECRET=your-secure-random-secret-here
